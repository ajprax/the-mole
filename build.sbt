name := "the-mole"

organization in ThisBuild := "com.goldblastgames"

version in ThisBuild := "0.1-SNAPSHOT"

scalaVersion in ThisBuild := "2.9.2"

scalacOptions in ThisBuild ++= Seq("-unchecked", "-deprecation")
