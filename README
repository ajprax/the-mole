Unnamed game project by GoldBlastGames

This project makes use of the echo library: https://github.com/oetzi/echo

To build & run the game's components:

$ cd lib/echo
$ sbt publish-local
$ cd ../..
$ sbt run
